<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds: allow HTTP to any host (emulator 10.0.2.2 + changing LAN IPs on physical device). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
