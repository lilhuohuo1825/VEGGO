# 📝 Nhật ký thay đổi (Change Log) - 16/06

Bản cập nhật này tập trung vào việc tối ưu hóa toàn diện giao diện người dùng (UI), trải nghiệm mua sắm (UX) và tích hợp các tính năng theo tiêu chuẩn thiết kế hiện đại (HIG - High-level Improvements/Guidelines).

---

## ✨ Cải tiến UI/UX & Tính năng mới (HIG)

### 1. Nâng cấp Trang Chi tiết Sản phẩm (`ProductDetailActivity`)
*   
*   **Quy trình mua hàng không gián đoạn (Add to Cart Bottom Sheet):**
    *   Sử dụng `BottomSheetDialog` hiện đại để chọn số lượng sản phẩm.
    *   **Real-time Calculation:** Tự động cập nhật tổng tiền ngay khi người dùng tăng/giảm số lượng.
    *   Tránh việc chuyển trang liên tục, giúp giữ chân người dùng tại trang sản phẩm.
*   **Hệ sinh thái nội dung liên quan:**
    *   **Related Recipes:** Gợi ý các công thức nấu ăn sử dụng sản phẩm hiện tại.
    *   **Related Products:** Hiển thị danh sách sản phẩm tương tự để tăng tỷ lệ chuyển đổi.
    *   **Consultation:** Mục tư vấn chuyên sâu giúp giải đáp thắc mắc khách hàng.
*   **Tối ưu trải nghiệm đọc:** Cơ chế **"Show More/Show Less"** cho mô tả sản phẩm dài, giữ giao diện luôn gọn gàng nhưng vẫn đầy đủ thông tin.

### 2. Hệ thống Danh mục & Điều hướng thông minh (`Category`)
*   **Phân loại đa cấp:** Hỗ trợ hiển thị Danh mục chính và Danh mục con (Subcategories) một cách mượt mà.
*   **Hệ thống cuộn ngang (Horizontal List):** Sử dụng `item_category_horizontal.xml` để hiển thị danh mục con, tối ưu không gian hiển thị trên thiết bị di động.
*   **Chuyển cảnh mượt mà:** Tách biệt `CategoryFragment` và `CategoryDetailFragment` để quản lý luồng dữ liệu chuyên sâu.

### 3. Thêm popup bộ lọc sản phẩm
*   **Popup Filter:** Thay thế giao diện lọc truyền thống bằng một cửa sổ Popup hiện đại, dễ thao tác.
*   **Material Design Chips:** 
    *   Sử dụng **Chips** cho các tiêu chí lọc (Giá, Xuất xứ, Loại).
    *   Tùy chỉnh trạng thái màu sắc (`chip_filter_text_color.xml`, `chip_filter_stroke_color.xml`) giúp người dùng nhận biết rõ ràng các tiêu chí đang được chọn.

---

## 🛠 Danh sách các file thay đổi (Detailed Files)

### 📂 Presentation / Logic (Java)
*   `ProductDetailActivity.java`: Logic chính cho Rating Summary, Bottom Sheet, và liên kết các Adapter nội dung.
*   `CategoryFragment.java` & `CategoryDetailFragment.java`: Xử lý hiển thị và điều hướng danh mục.
*   `CategoryAdapter.java` & `SubcategoryAdapter.java`: Quản lý danh sách danh mục đa cấp.

### 📂 Layouts (XML)
*   `fragment_category.xml` & `fragment_category_detail.xml`: Cấu trúc lại giao diện trang danh mục.
*   `item_category_horizontal.xml` & `item_subcategory.xml`: Thiết kế thẻ danh mục theo phong cách hiện đại.
*   `popup_filter.xml`: Layout cho bộ lọc sản phẩm.
*   `layout_add_to_cart_bottom_sheet.xml`: Giao diện chọn số lượng và tính tiền nhanh.

### 📂 Resources & Styling
*   `chip_filter_text_color.xml`: Định nghĩa màu text cho Chip (Selected vs Unselected).
*   `chip_filter_stroke_color.xml`: Định nghĩa màu viền cho Chip.
*   `CurrencyFormatter.java`: Utility định dạng tiền tệ VND đồng nhất toàn ứng dụng.

---
*Ghi chú: Bản cập nhật này đảm bảo tính nhất quán về mặt thị giác, tuân thủ nguyên tắc thiết kế Material Design và tối ưu hóa cho thao tác bằng một tay.*
